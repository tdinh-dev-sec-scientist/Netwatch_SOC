[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    slow: tests that run the full pipeline or a large workload
filterwarnings =
    error::DeprecationWarning:netwatch.*
