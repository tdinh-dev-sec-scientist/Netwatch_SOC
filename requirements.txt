# NetWatch SOC runtime dependencies.
# The capture, parsing, detection and persistence layers use only the Python
# standard library (struct, sqlite3, re, math). Flask serves the API and
# dashboard; gunicorn runs it in production.
Flask>=3.0,<4.0
gunicorn>=22.0,<24.0

# Development / testing
pytest>=8.0
